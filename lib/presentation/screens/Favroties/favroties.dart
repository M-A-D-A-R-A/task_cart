import 'package:flutter/material.dart';

class Favroties extends StatelessWidget {
  const Favroties ({ Key? key }) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container(
      
    );
  }
}